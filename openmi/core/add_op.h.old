#ifndef OPENMI_CORE_KERNELS_ADD_OP_H_
#define OPENMI_CORE_KERNELS_ADD_OP_H_  

#include "op_kernel.h"

namespace openmi {

class AddOp : public OpKernel {
public:
  void Initialize(OpKernelConstruction* context) override;

  void Compute(OpKernelContext* context) override;
}; // class AddOp

} // namespace openmi
#endif // OPENMI_CORE_KERNELS_ADD_OP_H_
